import { useEffect,useState } from "react"; 
import React from "react";
import { useSearchParams } from "react-router-dom"; 
import { api } from "../api"; 
import JobCard from "../components/JobCard";
export default function Jobs(){const [params,setParams]=useSearchParams();const [jobs,setJobs]=useState([]);const [loading,setLoading]=useState(true);const [form,setForm]=useState({search:params.get("search")||"",location:"",category:"",type:""});async function load(){setLoading(true);try{const r=await api.jobs(form);setJobs(r.jobs)}finally{setLoading(false)}}useEffect(()=>{load()},[]);return <main className="section jobs-page"><div className="container"><div className="page-heading"><span className="eyebrow">EXPLORE</span><h1>Find your next role</h1><p>Search across opportunities posted by employers on JobNest.</p></div><form className="filter-bar" onSubmit={e=>{e.preventDefault();setParams(form);load()}}><input placeholder="Job title, skill or company" value={form.search} onChange={e=>setForm({...form,search:e.target.value})}/><input placeholder="Location" value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/><select value={form.category} onChange={e=>setForm({...form,category:e.target.value})}><option value="">All categories</option><option>Engineering</option><option>Design</option><option>Marketing</option><option>Sales</option><option>Product</option></select><select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option value="">All types</option><option>Full-time</option><option>Part-time</option><option>Internship</option><option>Contract</option><option>Remote</option></select><button className="btn">Search</button></form><div className="results-head"><strong>{jobs.length} {jobs.length===1?"job":"jobs"} found</strong></div>{loading?<div className="empty">Loading jobs…</div>:jobs.length?<div className="job-list">{jobs.map(j=><JobCard key={j._id} job={j}/>)}</div>:<div className="empty">No jobs matched your search.</div>}</div></main>}
